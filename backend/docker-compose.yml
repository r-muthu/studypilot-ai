services:
  backend:
    build: .
    container_name: studypilot-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./chroma_db:/app/chroma_db

    restart: unless-stopped